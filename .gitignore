node_modules/
dist/
build/
.turbo/

# Base local: reproduzivel a partir da API do BCB via `pnpm sync`.
*.db
*.db-journal

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Anotacoes pessoais na raiz: rascunho de ideias, nao conteudo do projeto.
# `git add -A` varre tudo, e um arquivo assim num repositorio publico expoe
# o que ainda era so um pensamento.
/*.txt
